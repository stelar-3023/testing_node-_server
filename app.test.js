import supertest from "supertest";
import app from "./app.js";

describe("POST /users", () => {
  describe("given a username and password", () => {});

  descrcibe("when the username and password is missing", () => {});
});
